import React from "react"
import {CreateForm} from "./CreateForm"
// alll from validations done in that createform file just import here and used 
export const Createpost = () =>{
    return (
        <div>
            <CreateForm />
        </div>
    )
}